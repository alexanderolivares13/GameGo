<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GameGo</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div>
        <header class="bg-secondary">
            <div class="bg-secondary d-flex justify-content-between align-items-center">
                <a href="/" class="px-2">
                    <img src="/public/assets/logo.svg" alt="GameGo logo" height="80" width="200" class="block">
                </a>
                <nav class="d-flex flex-column">
                {{#if logged_in}}
                    <button type="button" class="btn btn-success" id="logout">logout</button>
                {{else}}
                    <a href="/login" class="py-1 px-2">
                    <button type="button" class="btn btn-success" id="login">login</button>
                    </a>
                {{/if}}
                    <a href="" class="py-1 px-2 d-flex justify-content-end">cart</a>
                </nav>
            </div>
        </header>
        <main class="background">
            {{{ body }}}
        </main>
        <footer>

        </footer>
    </div>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>